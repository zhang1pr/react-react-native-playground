{
  "name": "react-playground",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npm run dev --prefix react",
    "dev:react": "npm run dev --prefix react",
    "build:react": "npm run build --prefix react",
    "lint:react": "npm run lint --prefix react",
    "dev:react-native": "npm run start --prefix react-native",
    "android": "npm run android --prefix react-native",
    "ios": "npm run ios --prefix react-native",
    "web:native": "npm run web --prefix react-native"
  }
}
